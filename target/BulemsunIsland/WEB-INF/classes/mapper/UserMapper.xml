<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluemsun.island.dao.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="User">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="identifyId" column="identify_id" jdbcType="TINYINT"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="phoneNumber" column="phone_number" jdbcType="VARCHAR"/>
        <result property="sex" column="sex" jdbcType="TINYINT"/>
        <result property="signature" column="signature" jdbcType="VARCHAR"/>
        <result property="birthday" column="birthday" jdbcType="DATE"/>
        <result property="imageUrl" column="image_url" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,identify_id,username,
        `password`,`phone_number`,sex,
        signature,birthday,image_url
    </sql>

    <update id="updateImageUrl" parameterType="user">
        update bluemsun_island.tb_user
        set image_url = '${imageUrl}'
        where id = #{id}
    </update>

    <select id="select" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        # *
        <include refid="Base_Column_List"/>
        from tb_user
        where id = #{id,jdbcType=INTEGER}
    </select>

    <delete id="deleteById" parameterType="java.lang.Long">
        delete
        from tb_user
        where id = #{id,jdbcType=INTEGER}
    </delete>

    <insert id="insert" parameterType="User">
        insert into tb_user
            (username, password, phone_number)
        values (#{username}, #{password}, #{phoneNumber});
    </insert>

    <select id="queryOneByPhoneNumberAndPassword" parameterType="User" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from tb_user
        where phone_number = #{phoneNumber} and password = #{password};
    </select>

    <update id="updateUser" parameterType="User">
        update tb_user
        <set>
            <if test="username!=null and username!='' ">
                username=#{username},
            </if>
            <if test="password!=null and password!='' ">
                password=#{password},
            </if>
            <if test="phoneNumber!=null and phoneNumber!='' ">
                phone_number=#{phoneNumber},
            </if>
            <if test="sex!=null and sex!='' ">
                sex=#{sex},
            </if>
            <if test="signature!=null and signature!='' ">
                signature =#{signature},
            </if>
            <if test="birthday!=null and birthday!='' ">
                birthday =#{birthday}
            </if>
        </set>
        where id = #{id};
    </update>
</mapper>
